import { PricewithdiscountPipe } from './pricewithdiscount.pipe';

describe('PricewithdiscountPipe', () => {
  it('create an instance', () => {
    const pipe = new PricewithdiscountPipe();
    expect(pipe).toBeTruthy();
  });
});
